# Phase 7 — ECS Deployment

This phase focused on deploying the microservices to Amazon Elastic Container Service (ECS).

Task definitions were created for each microservice and services were configured to run containers within the ECS cluster.

## Tasks Completed

- Created ECS task definitions
- Configured container definitions
- Deployed services to the ECS cluster

Both microservices were successfully deployed and accessible through the load balancer.
